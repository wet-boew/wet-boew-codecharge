INSTALL NOTES

Application uses a username/password located in 
a config file to access the database.
Default username/password is application/password
Default database name is myproject

User level access control is provided using a table called user

Later, an init package should be added to select dynamically those dbms system parameters 